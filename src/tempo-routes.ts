import { RouteObject } from "react-router-dom";

const routes: RouteObject[] = [
  {
    path: "/tempobook/*",
    element: null, // This is a placeholder for Tempo's internal routing
  },
];

export default routes;
